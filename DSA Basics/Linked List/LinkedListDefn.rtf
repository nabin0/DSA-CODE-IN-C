 linked list: Linear linked list are like arrays in which elements are stored in non contiguous memory locations.
                                ----USES OF LL----

In arrays we have limited capacity to store elements and contiguous memory location is must, but in linked list we can overcome these problems.